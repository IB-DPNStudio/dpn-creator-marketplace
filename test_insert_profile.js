const { createClient } = require('@supabase/supabase-js'); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); async function run() { const { data, error } = await supabase.from('profiles').insert({ id: '26fd73d6-f6cd-4b7d-a3d3-ae7c61b163f4', email: 'ashwin.gangakhedkar@dentsu.com', full_name: 'Ashwin Gangakhedkar', role: 'super_admin' }); if(error) console.error('Error:', error); else console.log('Successfully inserted Ashwin into profiles!'); } run();
